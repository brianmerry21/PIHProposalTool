# Public DNS name pointing to this VPS
DOMAIN=example.com

# Flask session secret (set to a long random value)
SESSION_SECRET=replace-with-a-long-random-secret

# SQLite database stored on a named Docker volume mounted at /data
DATABASE_URL=sqlite:////data/pih_data.db
